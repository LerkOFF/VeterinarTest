{% extends "layout.twig" %}

{% block content %}
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <div>
            <div class="d-flex align-items-center gap-2">
                <span class="badge text-bg-light border">👤</span>
                <h1 class="h4 fw-bold m-0">Клиенты</h1>
            </div>
            <div class="muted small mt-1">Поиск владельцев, контакты, карточки</div>
        </div>

        <a class="btn btn-success btn-edit" href="/clients/create">+ Добавить клиента</a>
    </div>

    <div class="card app-card app-shadow mb-3">
        <div class="card-body p-3">
            <form method="get" action="/clients" class="row g-2 align-items-center">
                <div class="col-12 col-md-8">
                    <input class="form-control" type="text" name="q" value="{{ q|e }}" placeholder="Поиск: ФИО или телефон">
                </div>
                <div class="col-12 col-md-4 d-flex gap-2">
                    <button class="btn btn-primary w-100" type="submit">Найти</button>
                    <a class="btn btn-outline-secondary w-100" href="/clients">Сброс</a>
                </div>
            </form>
        </div>
    </div>

    <div class="card app-card app-shadow">
        <div class="card-body p-0">
            {% if clients is empty %}
                <div class="p-4 muted">Пока нет клиентов.</div>
            {% else %}
                <div class="list-group list-group-flush">
                    {% for c in clients %}
                        <div class="list-group-item py-3">
                            <div class="d-flex flex-wrap justify-content-between align-items-center gap-2">
                                <div>
                                    <div class="fw-bold">{{ c.full_name|e }}</div>
                                    <div class="muted small">
                                        {% if c.phone %}📞 {{ c.phone|e }}{% endif %}
                                        {% if c.phone and c.address %} • {% endif %}
                                        {% if c.address %}📍 {{ c.address|e }}{% endif %}
                                    </div>
                                </div>

                                <div class="d-flex gap-2">
                                    <a class="btn btn-outline-primary btn-sm" href="/clients/{{ c.id }}">Открыть</a>

                                    {# back_to_list приходит из роутинга и содержит текущую страницу списка #}
                                    <a class="btn btn-success btn-edit btn-sm"
                                       href="/clients/{{ c.id }}/edit?back={{ back_to_list|url_encode }}">
                                        Редактировать
                                    </a>

                                    <form method="post" action="/clients/{{ c.id }}/delete"
                                          onsubmit="return confirm('Удалить клиента и всех его питомцев/визиты?');">
                                        <button class="btn btn-danger btn-sm" type="submit">Удалить</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>

                {% if pagination is defined and pagination.totalPages > 1 %}
                    <div class="p-3 border-top bg-white">
                        <div class="d-flex flex-wrap align-items-center justify-content-between gap-2">
                            <div class="muted small">
                                Показано {{ pagination.from }}–{{ pagination.to }} из {{ pagination.total }}
                            </div>

                            <nav aria-label="Навигация по страницам">
                                <ul class="pagination mb-0">
                                    <li class="page-item {{ pagination.prevUrl ? '' : 'disabled' }}">
                                        <a class="page-link" href="{{ pagination.prevUrl ?? '#' }}">«</a>
                                    </li>

                                    {% for p in pagination.pages %}
                                        <li class="page-item {{ p.num == pagination.page ? 'active' : '' }}">
                                            <a class="page-link" href="{{ p.url }}">{{ p.num }}</a>
                                        </li>
                                    {% endfor %}

                                    <li class="page-item {{ pagination.nextUrl ? '' : 'disabled' }}">
                                        <a class="page-link" href="{{ pagination.nextUrl ?? '#' }}">»</a>
                                    </li>
                                </ul>
                            </nav>
                        </div>
                    </div>
                {% endif %}
            {% endif %}
        </div>
    </div>
{% endblock %}
