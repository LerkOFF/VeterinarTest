{% extends "layout.twig" %}

{% block content %}
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <h1 class="h4 fw-bold m-0">Карточка клиента</h1>
        <div class="d-flex gap-2">
            <a class="btn btn-outline-secondary" href="/clients">Назад</a>
            <a class="btn btn-success btn-edit" href="/clients/{{ client.id }}/edit?back=/clients/{{ client.id }}">Редактировать</a>
            <form method="post" action="/clients/{{ client.id }}/delete" onsubmit="return confirm('Удалить клиента и всех его питомцев/визиты?');">
                <button class="btn btn-danger" type="submit">Удалить</button>
            </form>
        </div>
    </div>

    <div class="card app-card app-shadow mb-3">
        <div class="card-body p-4">
            <div class="fw-bold fs-4 mb-3">{{ client.full_name|e }}</div>

            <div class="row g-3 kv">
                <div class="col-12 col-md-6">
                    <div class="label">Телефон</div>
                    <div class="value" style="font-size: 1.25rem;">
                        {{ client.phone ? client.phone|e : '—' }}
                    </div>
                </div>
                <div class="col-12 col-md-6">
                    <div class="label">Адрес</div>
                    <div class="value">{{ client.address ? client.address|e : '—' }}</div>
                </div>

                <div class="col-12">
                    <div class="label">Заметки</div>
                    <div class="value fw-normal">{{ client.notes ? client.notes|e : '—' }}</div>
                </div>
            </div>
        </div>
    </div>

    {# -------------------- БЫСТРЫЙ ВИЗИТ -------------------- #}
    <div id="quick-visit" class="card app-card app-shadow mb-3">
        <div class="card-body p-4">
            <div class="d-flex flex-wrap justify-content-between align-items-center gap-2">
                <div>
                    <div class="fw-bold">Быстро добавить визит</div>
                    <div class="muted small">Без перехода в карточку питомца</div>
                </div>

                <button class="btn btn-outline-primary"
                        type="button"
                        data-bs-toggle="collapse"
                        data-bs-target="#quickVisitForm"
                        aria-expanded="{{ quick_open ? 'true' : 'false' }}"
                        aria-controls="quickVisitForm">
                    {{ quick_open ? 'Скрыть' : 'Открыть форму' }}
                </button>
            </div>

            {% if quick_saved %}
                <div class="alert alert-success mt-3 mb-0">Визит сохранён.</div>
            {% endif %}

            {% if quick_errors %}
                <div class="alert alert-danger mt-3">
                    <div class="fw-bold mb-2">Проверьте поля:</div>
                    <ul class="mb-0">
                        {% for e in quick_errors %}<li>{{ e|e }}</li>{% endfor %}
                    </ul>
                </div>
            {% endif %}

            <div class="collapse {% if quick_open %}show{% endif %} mt-3" id="quickVisitForm">
                {% if pets is empty %}
                    <div class="alert alert-warning mb-0">
                        У клиента нет питомцев. Сначала добавьте питомца.
                    </div>
                {% else %}
                    <form method="post" action="/clients/{{ client.id }}/visits/quick#quick-visit" class="row g-3">
                        <div class="col-12 col-md-6">
                            <label class="form-label fw-semibold">Питомец *</label>
                            <select class="form-select" name="pet_id" required>
                                <option value="">— Выберите —</option>
                                {% set selectedPet = quick.pet_id|default('') %}
                                {% for p in pets %}
                                    {% set pid = p.id|default('') %}
                                    <option value="{{ pid }}"
                                            {% if selectedPet == pid %}selected{% endif %}>
                                        {{ p.name|e }}
                                        {% if p.species %} ({{ p.species|e }}{% if p.breed %}, {{ p.breed|e }}{% endif %}){% endif %}
                                    </option>
                                {% endfor %}
                            </select>
                        </div>

                        <div class="col-12 col-md-3">
                            <label class="form-label fw-semibold">Дата (ДД-ММ-ГГГГ) *</label>
                            <input class="form-control" name="visit_date" value="{{ quick.visit_date|default('')|e }}" placeholder="13-12-2025" required>
                        </div>

                        <div class="col-12 col-md-3">
                            <label class="form-label fw-semibold">Время (ЧЧ:ММ) *</label>
                            <input class="form-control" name="visit_time" value="{{ quick.visit_time|default('')|e }}" placeholder="17:30" required>
                        </div>

                        <div class="col-12">
                            <label class="form-label fw-semibold">Жалоба</label>
                            <textarea class="form-control" name="complaint" rows="2">{{ quick.complaint|default('')|e }}</textarea>
                        </div>

                        <div class="col-12 col-md-6">
                            <label class="form-label fw-semibold">Диагноз</label>
                            <textarea class="form-control" name="diagnosis" rows="2">{{ quick.diagnosis|default('')|e }}</textarea>
                        </div>

                        <div class="col-12 col-md-6">
                            <label class="form-label fw-semibold">Процедуры</label>
                            <textarea class="form-control" name="procedures" rows="2">{{ quick.procedures|default('')|e }}</textarea>
                        </div>

                        <div class="col-12">
                            <label class="form-label fw-semibold">Рекомендации</label>
                            <textarea class="form-control" name="recommendations" rows="2">{{ quick.recommendations|default('')|e }}</textarea>
                        </div>

                        <div class="col-12 d-flex gap-2">
                            <button class="btn btn-success btn-edit" type="submit">Сохранить визит</button>
                            <a class="btn btn-outline-secondary" href="/clients/{{ client.id }}#quick-visit">Сбросить</a>
                        </div>
                    </form>
                {% endif %}
            </div>
        </div>
    </div>
    {# -------------------- /БЫСТРЫЙ ВИЗИТ -------------------- #}

    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-2">
        <h2 class="h5 fw-bold m-0">Питомцы</h2>
        <a class="btn btn-success btn-edit" href="/clients/{{ client.id }}/pets/create">+ Добавить питомца</a>
    </div>

    <div class="card app-card app-shadow">
        <div class="card-body p-0">
            {% if pets is empty %}
                <div class="p-4 muted">У клиента пока нет питомцев.</div>
            {% else %}
                <div class="list-group list-group-flush">
                    {% for p in pets %}
                        <div class="list-group-item py-3">
                            <div class="d-flex flex-wrap justify-content-between align-items-center gap-2">
                                <div>
                                    <div class="fw-bold">{{ p.name|e }}</div>
                                    <div class="muted small">
                                        {% if p.species %}{{ p.species|e }}{% endif %}
                                        {% if p.species and p.breed %} • {% endif %}
                                        {% if p.breed %}{{ p.breed|e }}{% endif %}
                                        {% if (p.species or p.breed) and p.birth_date_view %} • {% endif %}
                                        {% if p.birth_date_view %}🎂 {{ p.birth_date_view|e }}{% endif %}
                                    </div>
                                </div>

                                <div class="d-flex gap-2">
                                    <a class="btn btn-outline-primary btn-sm" href="/pets/{{ p.id }}">Открыть</a>
                                    <a class="btn btn-success btn-edit btn-sm" href="/pets/{{ p.id }}/edit?back=/clients/{{ client.id }}">Редактировать</a>
                                    <form method="post" action="/pets/{{ p.id }}/delete" onsubmit="return confirm('Удалить питомца и все его визиты?');">
                                        <button class="btn btn-danger btn-sm" type="submit">Удалить</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}
