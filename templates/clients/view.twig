{% extends "layout.twig" %}

{% block content %}
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <h1 class="h4 fw-bold m-0">Карточка клиента</h1>
        <div class="d-flex gap-2">
            <a class="btn btn-outline-secondary" href="/clients">Назад</a>
            <a class="btn btn-success btn-edit" href="/clients/{{ client.id }}/edit?back=/clients/{{ client.id }}">Редактировать</a>
            <form method="post" action="/clients/{{ client.id }}/delete" onsubmit="return confirm('Удалить клиента и всех его питомцев/визиты?');">
                <button class="btn btn-danger" type="submit">Удалить</button>
            </form>
        </div>
    </div>

    <div class="card app-card app-shadow mb-3">
        <div class="card-body p-4">
            <div class="fw-bold fs-4 mb-3">{{ client.full_name|e }}</div>

            <div class="row g-3 kv">
                <div class="col-12 col-md-6">
                    <div class="label">Телефон</div>
                    <div class="value" style="font-size: 1.25rem;">
                        {{ client.phone ? client.phone|e : '—' }}
                    </div>
                </div>
                <div class="col-12 col-md-6">
                    <div class="label">Адрес</div>
                    <div class="value">{{ client.address ? client.address|e : '—' }}</div>
                </div>

                <div class="col-12">
                    <div class="label">Заметки</div>
                    <div class="value fw-normal">{{ client.notes ? client.notes|e : '—' }}</div>
                </div>
            </div>
        </div>
    </div>

    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-2">
        <h2 class="h5 fw-bold m-0">Питомцы</h2>
        <a class="btn btn-success btn-edit" href="/clients/{{ client.id }}/pets/create">+ Добавить питомца</a>
    </div>

    <div class="card app-card app-shadow">
        <div class="card-body p-0">
            {% if pets is empty %}
                <div class="p-4 muted">У клиента пока нет питомцев.</div>
            {% else %}
                <div class="list-group list-group-flush">
                    {% for p in pets %}
                        <div class="list-group-item py-3">
                            <div class="d-flex flex-wrap justify-content-between align-items-center gap-2">
                                <div>
                                    <div class="fw-bold">{{ p.name|e }}</div>
                                    <div class="muted small">
                                        {% if p.species %}{{ p.species|e }}{% endif %}
                                        {% if p.species and p.breed %} • {% endif %}
                                        {% if p.breed %}{{ p.breed|e }}{% endif %}
                                        {% if (p.species or p.breed) and p.birth_date_view %} • {% endif %}
                                        {% if p.birth_date_view %}🎂 {{ p.birth_date_view|e }}{% endif %}
                                    </div>
                                </div>

                                <div class="d-flex gap-2">
                                    <a class="btn btn-outline-primary btn-sm" href="/pets/{{ p.id }}">Открыть</a>
                                    <a class="btn btn-success btn-edit btn-sm" href="/pets/{{ p.id }}/edit?back=/clients/{{ client.id }}">Редактировать</a>
                                    <form method="post" action="/pets/{{ p.id }}/delete" onsubmit="return confirm('Удалить питомца и все его визиты?');">
                                        <button class="btn btn-danger btn-sm" type="submit">Удалить</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}
