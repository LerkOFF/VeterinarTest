{% extends "layout.twig" %}

{% block content %}
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <div>
            <div class="d-flex align-items-center gap-2">
                <span class="badge text-bg-light border">📒</span>
                <h1 class="h4 fw-bold m-0">Журнал визитов</h1>
            </div>
            <div class="muted small mt-1">Расписание и история приёмов по датам</div>
        </div>
    </div>

    <div class="card app-card app-shadow mb-3">
        <div class="card-body p-3">
            <form method="get" action="/visits/journal" class="row g-2 align-items-end">
                <div class="col-12 col-md-3">
                    <label class="form-label small muted">С</label>
                    <input class="form-control" type="date" name="from" value="{{ from|e }}">
                </div>
                <div class="col-12 col-md-3">
                    <label class="form-label small muted">По</label>
                    <input class="form-control" type="date" name="to" value="{{ to|e }}">
                </div>
                <div class="col-12 col-md-4">
                    <label class="form-label small muted">Поиск</label>
                    <input class="form-control" type="text" name="q" value="{{ q|e }}"
                           placeholder="ФИО, телефон, кличка, жалоба…">
                </div>
                <div class="col-12 col-md-2 d-flex gap-2">
                    <button class="btn btn-primary w-100" type="submit">Показать</button>
                    <a class="btn btn-outline-secondary w-100" href="/visits/journal">Сброс</a>
                </div>
            </form>
        </div>
    </div>

    <div class="card app-card app-shadow">
        <div class="card-body p-0">
            {% if days is empty %}
                <div class="p-4 muted">В выбранном диапазоне визитов нет.</div>
            {% else %}
                {% for day in days %}
                    <div class="border-bottom p-3 bg-light-subtle">
                        <div class="d-flex align-items-center justify-content-between flex-wrap gap-2">
                            <div class="fw-bold">
                                {{ day.date_view|e }}
                            </div>
                            <div class="muted small">
                                Визитов: {{ day.items|length }}
                            </div>
                        </div>
                    </div>

                    <div class="list-group list-group-flush">
                        {% for v in day.items %}
                            <div class="list-group-item py-3">
                                <div class="d-flex flex-wrap justify-content-between align-items-start gap-3">
                                    <div class="flex-grow-1">
                                        <div class="fw-bold">
                                            {% if v.visit_time %}🕒 {{ v.visit_time|e }}{% else %}🕒 —{% endif %}
                                            <span class="mx-2 muted">•</span>
                                            <a href="/clients/{{ v.client_id }}" class="text-decoration-none">
                                                👤 {{ v.client_full_name|e }}
                                            </a>
                                        </div>

                                        <div class="muted small mt-1">
                                            <a href="/pets/{{ v.pet_id }}" class="text-decoration-none">
                                                🐾 {{ v.pet_name|e }}
                                            </a>
                                            {% if v.pet_extra %} • {{ v.pet_extra|e }}{% endif %}
                                            {% if v.client_phone %} • 📞 {{ v.client_phone|e }}{% endif %}
                                        </div>

                                        {% if v.complaint %}
                                            <div class="mt-2">
                                                <span class="badge text-bg-light border">Жалоба</span>
                                                <span class="ms-1">{{ v.complaint|e }}</span>
                                            </div>
                                        {% endif %}

                                        {% if v.diagnosis %}
                                            <div class="mt-2">
                                                <span class="badge text-bg-light border">Диагноз</span>
                                                <span class="ms-1">{{ v.diagnosis|e }}</span>
                                            </div>
                                        {% endif %}
                                    </div>

                                    <div class="text-end d-flex flex-column gap-2">
                                    <span class="badge text-bg-light border align-self-end">
                                        №{{ loop.index }}
                                    </span>

                                        {# ✅ ВАЖНО: back = текущая страница журнала с фильтрами/страницей #}
                                        <a href="/visits/{{ v.id }}/edit?back={{ current_url|url_encode }}"
                                           class="btn btn-success btn-edit btn-sm">
                                            Редактировать
                                        </a>

                                        <a href="/pets/{{ v.pet_id }}" class="btn btn-outline-primary btn-sm">
                                            Питомец
                                        </a>

                                        <a href="/clients/{{ v.client_id }}" class="btn btn-outline-secondary btn-sm">
                                            Клиент
                                        </a>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% endfor %}

                {% if pagination is defined and pagination.totalPages > 1 %}
                    <div class="p-3 border-top bg-white">
                        <div class="d-flex flex-wrap align-items-center justify-content-between gap-2">
                            <div class="muted small">
                                Показано дней: {{ pagination.from }}–{{ pagination.to }} из {{ pagination.total }}
                            </div>

                            <nav aria-label="Навигация по страницам">
                                <ul class="pagination mb-0">
                                    <li class="page-item {{ pagination.prevUrl ? '' : 'disabled' }}">
                                        <a class="page-link" href="{{ pagination.prevUrl ?? '#' }}">«</a>
                                    </li>

                                    {% for pg in pagination.pages %}
                                        <li class="page-item {{ pg.num == pagination.page ? 'active' : '' }}">
                                            <a class="page-link" href="{{ pg.url }}">{{ pg.num }}</a>
                                        </li>
                                    {% endfor %}

                                    <li class="page-item {{ pagination.nextUrl ? '' : 'disabled' }}">
                                        <a class="page-link" href="{{ pagination.nextUrl ?? '#' }}">»</a>
                                    </li>
                                </ul>
                            </nav>
                        </div>
                    </div>
                {% endif %}
            {% endif %}
        </div>
    </div>
{% endblock %}
