{% extends "layout.twig" %}

{% block content %}
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <h1 class="h4 fw-bold m-0">Питомцы</h1>
    </div>

    <div class="card app-card app-shadow mb-3">
        <div class="card-body p-3">
            <form method="get" action="/pets" class="row g-2 align-items-center">
                <div class="col-12 col-md-8">
                    <input class="form-control" type="text" name="q" value="{{ q|e }}" placeholder="Поиск: кличка, вид, порода, клиент">
                </div>
                <div class="col-12 col-md-4 d-flex gap-2">
                    <button class="btn btn-primary w-100" type="submit">Найти</button>
                    <a class="btn btn-outline-secondary w-100" href="/pets">Сброс</a>
                </div>
            </form>
        </div>
    </div>

    <div class="card app-card app-shadow">
        <div class="card-body p-0">
            {% if pets is empty %}
                <div class="p-4 muted">Пока нет питомцев.</div>
            {% else %}
                <div class="list-group list-group-flush">
                    {% for p in pets %}
                        <div class="list-group-item py-3">
                            <div class="d-flex flex-wrap justify-content-between align-items-center gap-2">
                                <div>
                                    <div class="fw-bold">{{ p.name|e }}</div>
                                    <div class="muted small">
                                        {% if p.client_full_name %}👤 {{ p.client_full_name|e }}{% endif %}
                                        {% if p.client_full_name and (p.species or p.breed) %} • {% endif %}
                                        {% if p.species %}{{ p.species|e }}{% endif %}
                                        {% if p.species and p.breed %} • {% endif %}
                                        {% if p.breed %}{{ p.breed|e }}{% endif %}
                                        {% if (p.species or p.breed) and p.birth_date_view %} • {% endif %}
                                        {% if p.birth_date_view %}🎂 {{ p.birth_date_view|e }}{% endif %}
                                    </div>
                                </div>

                                <div class="d-flex gap-2">
                                    <a class="btn btn-outline-primary btn-sm" href="/pets/{{ p.id }}">Открыть</a>
                                    <a class="btn btn-success btn-edit btn-sm" href="/pets/{{ p.id }}/edit?back=/pets">Редактировать</a>
                                    <form method="post" action="/pets/{{ p.id }}/delete" onsubmit="return confirm('Удалить питомца и все его визиты?');">
                                        <button class="btn btn-danger btn-sm" type="submit">Удалить</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}
